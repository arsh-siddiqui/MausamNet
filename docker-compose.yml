# MausamNet prototype compose — intentionally minimal.
# The prototype runs fully locally (SQLite + demo connectors).
# Future services can be added here as real connectors come online.
version: "3.9"
services:
  # Future MongoDB:
  # mongo:
  #   image: mongo:7
  #   ports: ["27017:27017"]
  #   volumes: ["mongo_data:/data/db"]
  #   restart: unless-stopped
  #
  # Future Redis (rate limiting / cache):
  # redis:
  #   image: redis:7-alpine
  #   ports: ["6379:6379"]
  placeholder:
    image: busybox
    command: ["echo", "MausamNet prototype runs locally. Uncomment services as needed."]
